"""OpenAIEmbedder — optional provider using text-embedding-ada-002 (ADR 0003).

Only needed to reproduce the reference paper's published dense-baseline numbers; requires
OPENAI_API_KEY / AZURE_* credentials (see .env.example). Implements the base Embedder interface.

STUB.
"""

raise NotImplementedError("embedding.openai_embed: OpenAIEmbedder not implemented yet")
