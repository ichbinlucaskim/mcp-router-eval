"""Query slicing by closure depth (ADR 0005): shallow (2-3 gold tools) vs deep (>=6).

Replaces the single-vs-composite split because ToolLinkOS has zero single-tool queries; depth
still isolates the composite regime where the GNN is expected to win.

STUB.
"""

raise NotImplementedError("eval.slices: not implemented yet (T0.3)")
