"""Router interface: route(query) -> RouteResult (§3.1).

Every router (baseline or GNN) implements this so the contract + executor stay fixed and only
the router is swapped (ablation A). Emits ranked_tools, selected_tools + closure, confidence,
and homophily_local.

STUB.
"""

raise NotImplementedError("routers.base: Router interface not implemented yet")
